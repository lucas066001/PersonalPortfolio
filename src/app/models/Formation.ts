export class Formation{
    title:string;
    description:string;

    constructor(givenTitle:string, givenDescription:string) {
        this.title = givenTitle;
        this.description = givenDescription;
      }
}