export class Hobbie{
    iconPath:string;
    description:string;

    constructor(givenIconPath:string, givenDescription:string) {
        this.iconPath = givenIconPath;
        this.description = givenDescription;
      }
}